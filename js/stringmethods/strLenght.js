const str = "code explained"

console.log(str.length); //...gives the length of the string
console.log(str[5]); //... we can use to find the element at a given index
console.log(str[str.length-1]); //... we can use to find the last element

//....we can also use charAt() forthe same

console.log(str.charAt(3));